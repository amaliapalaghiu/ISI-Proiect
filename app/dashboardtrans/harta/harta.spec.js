'use strict';

describe('myApp.hartaTrans module', function() {

  beforeEach(module('myApp.hartaTrans'));

  describe('hartaTrans controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var hartaCtrl = $controller('HartaTransCtrl');
      expect(hartaCtrl).toBeDefined();
    }));

  });
});