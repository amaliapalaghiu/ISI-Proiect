'use strict';

describe('myApp.oferteTrans module', function() {

  beforeEach(module('myApp.oferteTrans'));

  describe('oferteTrans controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var oferteCtrl = $controller('OferteTransCtrl');
      expect(oferteCtrl).toBeDefined();
    }));

  });
});