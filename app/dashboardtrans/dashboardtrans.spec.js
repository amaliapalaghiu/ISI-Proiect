'use strict';

describe('myApp.dashboardtrans module', function() {

  beforeEach(module('myApp.dashboardtrans'));

  describe('dashboardtrans controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var dashboardCtrl = $controller('DashboardTransCtrl');
      expect(dashboardCtrl).toBeDefined();
    }));

  });
});