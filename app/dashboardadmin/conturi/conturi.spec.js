'use strict';

describe('myApp.conturi module', function() {

  beforeEach(module('myApp.conturi'));

  describe('conturi controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var conturiCtrl = $controller('ConturiCtrl');
      expect(conturiCtrl).toBeDefined();
    }));

  });
});