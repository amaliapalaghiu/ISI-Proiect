'use strict';

describe('myApp.dashboardexp module', function() {

  beforeEach(module('myApp.dashboardexp'));

  describe('dashboardexp controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var dashboardCtrl = $controller('DashboardExpCtrl');
      expect(dashboardCtrl).toBeDefined();
    }));

  });
});