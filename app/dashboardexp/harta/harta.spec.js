'use strict';

describe('myApp.hartaExp module', function() {

  beforeEach(module('myApp.hartaExp'));

  describe('hartaExp controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var hartaCtrl = $controller('HartaExpCtrl');
      expect(hartaCtrl).toBeDefined();
    }));

  });
});