'use strict';

describe('myApp.cereriExp module', function() {

  beforeEach(module('myApp.cereriExp'));

  describe('cereriExp controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var cereriCtrl = $controller('CereriExpCtrl');
      expect(cereriCtrl).toBeDefined();
    }));

  });
});