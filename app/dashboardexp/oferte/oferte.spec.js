'use strict';

describe('myApp.oferteExp module', function() {

  beforeEach(module('myApp.oferteExp'));

  describe('oferteExp controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var oferteCtrl = $controller('OferteExpCtrl');
      expect(oferteCtrl).toBeDefined();
    }));

  });
});