'use strict';

describe('myApp.success module', function() {

  beforeEach(module('myApp.success'));

  describe('SuccessCtrl controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var successCtrl = $controller('SuccessCtrl');
      expect(successCtrl).toBeDefined();
    }));

  });
});