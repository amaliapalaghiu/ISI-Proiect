'use strict';

describe('myApp.cererileMele module', function() {

  beforeEach(module('myApp.cererileMele'));

  describe('cererileMele controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var cereriCtrl = $controller('CererileMeleCtrl');
      expect(cereriCtrl).toBeDefined();
    }));

  });
});