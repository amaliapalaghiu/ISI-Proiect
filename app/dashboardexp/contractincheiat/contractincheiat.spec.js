'use strict';

describe('myApp.contractIncheiatExp module', function() {

  beforeEach(module('myApp.contractIncheiatExp'));

  describe('contractIncheiatExp controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var oferteCtrl = $controller('ContractIncheiatExpCtrl');
      expect(oferteCtrl).toBeDefined();
    }));

  });
});